extern crate wasmy_flux;

use wasmy_flux::parse_query;

fn main() {
    let query = "from db(\"telegraf\")
|> range start: -1m)
|> filter(fn: (r) => r._measurement == \"m\")";

    match parse_query(query) {
        Ok(_) => {},
        Err(e) => println!("{}", e)
    }
}
